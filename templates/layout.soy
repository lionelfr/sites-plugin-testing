{namespace landing}

/**
 * @param baseUrl
 * @param locale
 * @param siteDomain
 * @param siteInternalHostName
 * @param schema
 */
{template .layout}
    {$baseUrl}
    {$locale}
    {$siteDomain}
    {$siteInternalHostName}
    {$schema}
{/template}